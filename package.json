{
  "name": "germany-meds-fullstack",
  "version": "1.0.0",
  "description": "Germany Meds Full Stack Application",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev"
  },
  "workspaces": [
    "client"
  ]
}
